# 알고리즘 수업 정리 2주차

# Ch01-Sec01 자료구조의 이해

---

### **1. 자료구조의 개념**

**:** 자료를 효율적으로 표현하고 저장하고 처리할 수 있도록 정리하는 것.

- 사용자(프로그래머)는 문제 도출 단계에서 문제를 더 효율적이고 효과적으로 해결하기 위해 문제를 정의.
- 문제 변환 단계에서는 처리 방식을 결정하여 알고리즘을 작성하고 처리 대상을 결정한 뒤 그에 대한 자료를 정의.

**자료구조에서 다루는 내용**

: 이론적 측면

- 그래프 이론, 집합 이론, 조합적 분석의 이산 수학과 확률 이론을 기본으로 알고리즘을 분석하여 검색, 정렬 방법 등을 결정
- 공간 복잡도와 시간 복잡도를 기준으로 효율 분석을 통해 최적의 상태를 결정. (최근에는 공간 복잡도 보다 시간 복잡도를 중요시 여긴다.)

: 실제적 측면

이론적으로 결정한 내용에 따라 자료를 문자열, 리스트, 트리, 그래프, 파일 등의 구조로 표현하고 알고리즘을 구현하여, 프로그래밍과 파일 작성 및 메모리 관리와 운영체제 등에 사용.

### **2. 자료구조의 분류**

: 표현할 자료의 특성과 주된 사용 방법, 수행하는 연산의 특성, 구현에 필요한 저장 공간 용량과 실행 소요 시간 등을 고려해 가장 효율적인 자료구조를 선택.

- 단순 구조
    - 자료값을 사용하기 위한 기본 형태.
    - 정수, 실수, 문자, 문자열 등의 데이터형에 해당(Data Type).
- 선형 구조 (자료 사이의 관계가 1:1)
    - 순차 리스트 (Sequential List) : 자료의 논리적인 순서와 기억 장소에 저장되는 물리적 순서가 일치하는 구조.
    - 연결 리스트 (Linked List) : 물리적인 순서와 상관없이 포인터를 사용하여 논리적인 순서대로 연결하는 구조.
    - 스택, 큐, 데크 (Stack, Queue, Deque) : 자료의 삽입이나 삭제 위치에 대한 제한 조건이 있는 리스트.
- 비선형 구조 (자료 사이의 관계가 1:n)
    - 계층 구조나 망 구조를 갖는 자료구조로 트리(Tree)와 그래프(Graph)가 있음.
- 파일구조
    - 서로 관련 있는 필드로 구성된 레코드 집합인 파일에 대한 자료구조로 보조 기억장치에 데이터가 실제로 기록되는 형태.
    - 파일에 구성 방식에 따라 순차 파일, 색인(인덱스) 파일, 직접 파일 등.

# Sec02 자료의 표현

---

## **1. 컴퓨터에서의 자료 표현**

: 컴퓨터는 자료를 표현하기 위해 1과 0(ON과 OFF, 참과 거짓)의 조합으로 구성된 2진수 코드를 사용.

- 비트 : 2진수 한자리인 1 또는 0을 표현하는 단위. → 자료를 표현하는 최소단위.
    - 4비트 : 니블, 8비트 : 1바이트.
- n비트로 2의 n승 개의 상태를 표현할 수 있다. ex) 2비트는 2의 2승해서 4개, 4비트는 2의 4승해서 16개
- 컴퓨터 내부에서 표현할 수 있는 자료 : 수치 자료, 문자 자료, 논리 자료, 포인터 자료, 문자열 자료 등
    
    → 모든 자료가 1과 0의 조합인 2진수로 표현.
    

## 2. 수치 자료의 표현

### [10진수의 2진수 표현 — 존(Zone) 형식]

- 10진수 한 자리를 **존(Zone) 형식**으로 표현하기 위해 **1바이트(8비트)**를 한 단위로 사용
- 1바이트 구성: **상위 4비트(존 영역)** + **하위 4비트(수치 영역)**
    - 존 영역: 항상 **1111** 고정
    - 수치 영역: 4비트로 10진수 **0~15** 표현 (0000~1111)
    - 10에서 15까지의 두 자리 10진수는 **A~F**의 문자를 사용하여 한 자리로 표현

**부호 표현 (존 형식)**

- 부호는 **최하위 바이트의 존 영역**에 표시
    - 양수(+): `1100` = **C**
    - 음수(−): `1101` = **D**

### 예시: +213과 −213

| 값 | F | 2 | F | 1 | C(+) 또는 D(−) | 3 |
| --- | --- | --- | --- | --- | --- | --- |
| +213 | 1111 | 0010 | 1111 | 0001 | **1100** | 0011 |
| −213 | 1111 | 0010 | 1111 | 0001 | **1101** | 0011 |

> 💡 F는 존 영역(1111)을 뜻하고, 마지막 숫자(최하위 바이트)의 존 영역에만 부호(C/D)가 들어감
> 

---

### [10진수의 2진수 표현 — 팩(Pack) 형식]

- 존 형식의 문제: 숫자만 사용하는 연산에서 존 영역을 저장해야 하므로 기억 공간이 낭비됨
- 팩 형식: **1바이트에 10진수 두 자리**를 표현 → 기억 공간 절약
- 구성: 숫자는 각 4비트씩, **부호는 최하위 바이트의 하위 4비트(S 영역)**에 표시
    - 양수(+): `1100` = C
    - 음수(−): `1101` = D

**예시: +213과 −213**

| 값 | 2 | 1 | 3 | 부호 |
| --- | --- | --- | --- | --- |
| +213 | 0010 | 0001 | 0011 | **1100** |
| −213 | 0010 | 0001 | 0011 | **1101** |

---

### [2진수의 정수 표현]

2진수는 일정한 길이의 n비트로 표현하며, **최상위 비트(MSB)** 첫 번째 비트는 부호를 표시한다.

- 2진수로 정수를 표현하는 방법: **부호를 어떻게 표현하느냐**에 따라 3가지로 나뉨

**① 부호와 절댓값 형식**

- 최상위 비트: **부호** (양수=0, 음수=1)
- 나머지 비트: **절댓값** (2진수의 절댓값)
- 1바이트 사용 시: 첫 1비트(부호) + 나머지 7비트(절댓값)

**특징**
- MSB만 바꿔 음수를 간단히 표현 가능
- 가산기와 감산기가 모두 필요 → 설계 비용이 많이 듦
- +0(00000000)과 −0(10000000)이 존재 → 논리적으로 맞지 않음
- n비트로 −(2ⁿ⁻¹−1) ~ +(2ⁿ⁻¹−1) 범위 표현

**② 1의 보수 형식**

- 양수(+): 부호와 절댓값 형식과 동일
- 음수(−): 2진수를 **1의 보수**로 변환 (모든 비트를 반전)
    - n비트의 1의 보수 = (2ⁿ−1) − |x|
    - 예: −21을 8비트 1의 보수로 → 11101010

**특징**
- (A−B) 뺄셈을 (A+B의 1의 보수)로 변환해 가산기만으로 감산 수행 가능
- +0(00000000)과 −0(11111111)이 존재 → 논리적으로 맞지 않음
- n비트로 −(2ⁿ⁻¹−1) ~ +(2ⁿ⁻¹−1) 범위 표현

**③ 2의 보수 형식**

- 양수(+): 부호와 절댓값 형식과 동일
- 음수(−): 2진수를 **2의 보수**로 변환
    - n비트의 2의 보수 = 2ⁿ − |x|
    - = 1의 보수 + 1
    - 예: −21을 8비트 2의 보수로 → 11101011

**특징**
- (A−B) 뺄셈을 (A+B의 2의 보수)로 변환해 가산기만으로 감산 수행 가능
- 덧셈 연산에서 발생하는 오버플로를 1의 보수 형식보다 간단히 처리
- **컴퓨터 시스템에서 실제로 사용하는 형식**
- +0과 −0이 구분되지 않음 (0은 하나만 존재)
- n비트로 −2ⁿ⁻¹ ~ +(2ⁿ⁻¹−1) 범위 표현

**세 가지 방법 비교 (8비트 기준)**

| 8비트 2진수 | 부호와 절댓값 형식 | 1의 보수 형식 | 2의 보수 형식 |
| --- | --- | --- | --- |
| 01111111 | +127 | +127 | +127 |
| 00000001 | +1 | +1 | +1 |
| 00000000 | +0 | +0 | 0 |
| 10000000 | −0 | −127 | −128 |
| 10000001 | −1 | −126 | −127 |
| 11111111 | −127 | −0 | −1 |
| **표현 범위** | −127 ~ +127 | −127 ~ +127 | **−128 ~ +127** |

---

### [2진수의 실수 표현]

- 실수: 정수부와 소수부 사이에 소수점이 있는 숫자
- 컴퓨터는 2진수만으로 실수를 표현해야 하므로 **소수점을 직접 표현하지 못하고**, 정수부와 실수부의 위치를 정의하여 표현

**① 고정소수점 표현 방식**

- 소수점 위치가 **항상 고정**
- 소수점이 항상 최상위 비트 왼쪽 밖에 고정 → 취급값은 0.xxx 형태
- 소수점이 항상 최하위 비트 오른쪽 밖에 고정 → 결국 정수 표현과 같음

**② 부동소수점 표현 방식**

- **과학적 표기법** 방식 사용 (예: 213 = 0.213 × 10³)
- 고정소수점에 비해 **아주 작은 값이나 아주 큰 값** 표현 가능
- 현재 컴퓨터는 **IEEE 754 표준** 부동소수점 표현 형식 사용
- 세 가지 영역으로 구성: **부호(S)**, **지수부**, **가수부**
    - **단정도(Single Precision)**: 4바이트(32비트)
    - **배정도(Double Precision)**: 8바이트(64비트)

**IEEE 754 부동소수점 표현 과정 (예: 100010.101)**

1. **정규화**: 정수부가 1이 되도록 소수점을 이동 → `1.00010101 × 2⁵`
2. **부호**: 양수=0, 음수=1 저장
3. **가수부**: 정규화 후 정수부(1)를 생략하고 소수부(00010101)만 저장
4. **지수부**: 정규화한 지수와 **바이어스(Bias)** 값을 더해 저장
    - 단정도: 바이어스 = **127** → 지수 5+127=132=10000100₂
    - 배정도: 바이어스 = **1023** → 지수 5+1023=1028=10000000100₂

## 3. 문자 자료의 표현

컴퓨터 내부에서 문자 자료는 **1과 0의 2진수 조합**으로 표현한다.

주로 사용하는 문자 코드: **BCD 코드 / EBCDIC 코드 / ASCII 코드 / 유니코드**

---

### [BCD 코드 (Binary-Coded Decimal)]

- **6비트** 사용
- 구성 : **상위 2비트 (존 비트)** + **하위 4비트 (숫자 비트)**
- 표현 범위 : 0~9 숫자, 영어 대문자, 특수문자

| 존 비트 (AB) | 의미 |
| --- | --- |
| 00 | 숫자 0~9 (1010 ~ 1001) |
| 01 | 문자 A~I (0001 ~ 1001) |
| 10 | 문자 J~R (0001 ~ 1001) |
| 11 | 문자 S~Z (0010 ~ 1001) |

> 예) A = 존 비트 `01` + 숫자 비트 `0001` → `01 0001`
> 

---

### [EBCDIC 코드 (Extended Binary-Coded Decimal Interchange Code)]

- IBM에서 개발
- **8비트** 사용
- 구성 : **상위 4비트 (존 비트)** + **하위 4비트 (숫자 비트)**
- 표현 범위 : 0~9 숫자, 영어 대/소문자, 특수문자

| 존 비트 (AB) | 의미 |
| --- | --- |
| 00 | 여분 |
| 01 | 특수문자 |
| 10 | 영어 소문자 |
| 11 | 영어 대문자 |

| 존 비트 (CD) | 의미 |
| --- | --- |
| 00 | 문자 A~I (0001 ~ 1001) |
| 01 | 문자 J~R (0001 ~ 1001) |
| 10 | 문자 S~Z (0010 ~ 1001) |
| 11 | 숫자 0~9 (0000 ~ 1001) |

> EBCDIC 코드 표에서 **열 = 상위 4비트(존 비트)**, **행 = 하위 4비트(숫자 비트)**
> 
> 
> 열과 행의 비트를 연결하면 8비트의 EBCDIC 코드를 구할 수 있다.
> 
> 예) A가 있는 자리 : 열(존 비트) `1100` + 행(숫자 비트) `0001` → `11000001`
> 

---

### [ASCII 코드 (American Standard Code for Information Interchange)]

- **7비트** 사용
- 구성 : **상위 3비트 (존 비트)** + **하위 4비트 (숫자 비트)**
- 표현 범위 : 0~9 숫자, 영어 대/소문자, 특수문자
- 데이터 통신용으로 사용할 때 : 최상위 비트에 **패리티 비트**를 추가해 8비트로 사용

> ASCII 코드 표에서 **열 = 상위 3비트(존 비트)**, **행 = 하위 4비트(숫자 비트)**
> 
> 
> 예) A가 있는 자리 : 열(존 비트) `100` + 행(숫자 비트) `0001` → `1000001`
> 

**사용처**
- 전자회로, 마이크로프로세서 등에서 사용
- C 프로그램에서 문자 코드로 사용

---

### [유니코드 (Unicode)]

- EBCDIC, ASCII 코드는 최대 8비트로 세계 여러 나라의 언어를 모두 표현하기 어려움
- 이를 해결하기 위해 국제 표준 코드 **(ISO/IEC 10646)** 인 유니코드를 제정
- **2바이트(16비트)** 조합 → 1바이트로 표현할 수 없었던 다양한 언어 표현 가능
- XML, Java, CORBA 3.0, WML 등 **인터넷 기반 프로그램과 제품**에 사용

**문자 코드의 역사 흐름**

| 순서 | 내용 |
| --- | --- |
| 초기 IBM | BCD 코드 사용 |
| 이후 | 더 많은 문자 표현을 위해 EBCDIC 코드로 대체 |
| 미국 표준 | ASCII 코드가 일반화 |
| 현재 | 표현 한계를 극복한 **유니코드**가 일반화 |

> BCD 코드는 현재 세븐 세그먼트를 이용해 숫자를 10진 코드로 출력하는 전자회로와 마이크로프로세서 등에서 사용됨.
> 

---

## 4. 논리 자료의 표현

- 논리 자료 : **참(True)과 거짓(False)** 중 하나를 나타내는 자료 형식
- 1비트로 표현 가능하지만, 일반적으로 컴퓨터 내부에서는 **1바이트(1워드)** 를 논리값 표현 단위로 사용

**1바이트로 논리 자료를 표현하는 3가지 방법**

| 방법 | 참(True) | 거짓(False) |
| --- | --- | --- |
| ① 최하위 비트를 1로 표시 | `00000001` | `00000000` |
| ② 전체 비트를 1로 표시 | `11111111` | `00000000` |
| ③ 하나 이상의 비트를 1로 표시 | 1인 비트가 있으면 참 | `00000000` |

---

## 5. 포인터 자료의 표현

- 포인터 자료 : **메모리 주소를 표현**하기 위한 자료 형식
- 자료를 저장하고 있는 변수나 특정 위치의 **메모리 주소를 저장**하며, 주소 연산에 사용
- 포인터 자료를 사용하면 복잡한 자료구조 연산을 **메모리에서의 주소 연산만으로 처리** 가능

---

## 6. 문자열 자료의 표현

- 문자열 자료 : 한 글자만 표현하는 문자 자료와 달리, **여러 글자로 이루어진 문자 그룹**을 하나의 자료로 취급
- 메모리에 **연속적으로 저장**하는 자료 형식
- 문자열 하나는 부분 문자열을 여러 개 포함할 수 있다

**부분 문자열을 포함하는 문자열을 메모리에 저장하는 3가지 방법**

| 방법 | 메모리 이용률 | 부분 문자열 탐색 시간 |
| --- | --- | --- |
| ① 구분자를 사용하는 방법 | 문자열 길이 + 구분자 길이 → **효율적** | 문자 비교 연산 시간 + 구분자 식별 시간 → **비효율적** |
| ② 고정 길이로 저장하는 방법 | 가장 긴 부분 문자열 길이 × 부분 문자열 개수 → **비효율적** | 문자 비교 연산 시간 → **효율적** |
| ③ 포인터를 사용하는 방법 | 문자열 길이 + 포인터 저장 공간 → **효율적** | 문자 비교 연산 시간 + 포인터 주소 연산 시간 → **효율적** |

**각 방법의 저장 구조 (문자열 ex) COMPUTER, DATA STRUCTURE, STRING)**

- **(a) 구분자 방법** : 하나의 연속된 공간에 모든 문자를 저장하고 구분자(`:`)로 경계를 구분
- **(b) 고정 길이 방법** : 각 부분 문자열을 같은 고정 길이의 공간에 저장. 남는 공간은 미사용 공간으로 낭비됨
- **(c) 포인터 방법** : 문자열을 연속으로 저장하고, 각 부분 문자열의 시작 주소와 마지막 주소를 포인터에 저장하여 포인터 주소 연산으로 부분 문자열을 탐색

# Sec03 자료의 추상화

---

### 추상화란?

인간은 복잡한 문제를 풀기 위해 **추상화(Abstraction)** 를 이용한다.

- 추상화 : 자세하고 복잡한 것 대신, **필수적이고 중요한 특징만 골라 단순화**시키는 작업(What)
- 구체화 : 추상화된 것을 사용하기 위해 **’어떻게 할지’를 실제로 표현**하는 것(How)

> 예) ’고리’를 추상화된 기본 틀로 보면, 귀걸이·코걸이·배꼽걸이는 각각의 구체화에 해당한다.
> 

컴퓨터에서 문제 해결 시 : 큰 복잡한 문제에 추상화를 적용해 단순화 → 해결 방법을 쉽게 찾을 수 있다.

---

### 자료 추상화 (Data Abstraction)

자료 추상화는 **이미 알고 있는 잘 정의된 기본 개념**을 이용하여 표현한다.

**자료 추상화의 기본 개념 3가지**

| 개념 | 설명 |
| --- | --- |
| **자료 (Data)** | 프로그램의 처리 대상이 되는 모든 것. 어떤 값(Value) 자체를 의미하기도 함 |
| **연산 (Operation)** | 어떤 일을 처리하는 과정으로 연산자를 이용하여 수행. 예) 덧셈 연산 = + 연산자로 수행 |
| **자료형 (Data Type)** | 처리할 자료의 집합과 자료에 대해 수행할 수 있는 연산자의 집합 |

---

### 자료형 (Data Type)

자료형을 정의할 때는 자료형에 속하는 **값과 처리하기 위해 사용할 수 있는 연산자**를 정의한다.

> 예) 정수 자료형의 값 : `..., -1, 0, 1, ...` / 연산자 : `+, -, ×, ÷, mod`
> 

**자료형의 종류**

| 종류 | 설명 |
| --- | --- |
| 시스템 정의 자료형 | 프로그래밍 언어에서 정의하여 제공하는 자료형 |
| 사용자 정의 자료형 | 사용자가 필요에 따라 시스템 정의 자료형이나 기존의 사용자 정의 자료형을 이용하여 정의한 것 |

**자료형을 정의하는 올바른 방법**

자료형을 구체적으로 구현하기 전에 자료형에 대한 **자료의 특성, 연산자, 연산자가 수행하는 내용 등을 논리적으로 먼저 정의**해야 한다.

→ 이렇게 추상화하여 정의한 자료형 = **추상 자료형 (ADT, Abstract Data Type)**

- 추상 자료형은 구체적인 구현을 포함하지 않기 때문에 문제 해결 알고리즘 개발이 훨씬 단순해짐
- 이후 원하는 프로그래밍 언어를 사용하여 프로그램으로 구체화하여 실행함

---

### 자료와 연산의 추상화와 구체화

| 구분 | 자료 | 연산 |
| --- | --- | --- |
| **추상화** | 추상 자료형 | 알고리즘 정의 |
| **구체화** | 자료형 | 프로그램 구현 |

---

### 컴퓨팅 사고 (Computational Thinking)

| 구분 | 설명 |
| --- | --- |
| **추상화** | 복잡하게 얽혀 있는 문제를 구조화하고 작은 문제로 분해하여 해결 가능한 간결한 상태로 만드는 것 |
| **자동화** | 추상화된 문제 자료를 표현하고 알고리즘을 컴퓨터 언어로 바꾸어 실행하는 것 |

**컴퓨팅 사고의 문제 해결 흐름**

| 단계 | 내용 |
| --- | --- |
| 추상화 단계 (사람에 의해 수행) | 자료 수집 → 자료 분석 → 자료 표현 → 문제 해결 → 추상화 → 알고리즘 및 절차 |
| 자동화 단계 (컴퓨터에 의해 수행) | 자동화 → 시뮬레이션 → 병렬화·동시 실행 |

> 컴퓨팅 사고는 컴퓨터가 사고하는 방식이 아니라, **인간이 문제를 해결하는 컴퓨테이션적 방식**이다.
> 
> 
> 컴퓨터/소프트웨어 전공자면 늘 해 왔던 자연스러운 사고 방식이다.
> 

# Sec04 알고리즘의 이해

---

### **알고리즘의 개념**

**:** 주어진 문제를 해결하는 방법을 추상화하여 일련의 단계적 절차를 논리적으로 기술해 놓은 명세서.

- 자료를 정의하고, 그 자료를 처리하기에 적절한 알고리즘을 작성해야 문제를 효과적으로 해결 가능.

**알고리즘이 만족해야 하는 조건**

- **입력** : 알고리즘을 수행하는 데 필요한 자료가 외부에서 입력되어야 한다.
- **출력** : 알고리즘을 수행하고 나면 결과를 하나 이상 출력해야 한다.
- **명확성** : 수행할 작업의 내용과 순서를 나타내는 알고리즘의 명령어는 명확하게 명세되어야 한다.
- **유한성** : 알고리즘을 모두 수행하고 나면 반드시 종료되어야 한다.
- **효과성** : 알고리즘의 모든 명령어는 기본적이며 실행할 수 있어야 한다.

# Sec05 알고리즘의 표현 방법

---

### **1. 알고리즘 표현 방법의 종류**

| 방법 | 설명 | 한계 |
| --- | --- | --- |
| **자연어**를 이용한 서술적 표현 | 사람이 쓰는 자연어(언어)로 표현 | 일관성이나 명확성 유지가 어렵고, 누구나 이해할 수 있는 알고리즘 표현에 한계 있음 |
| **순서도**를 이용한 도식화 | Flow Chart를 작성하는 규칙에 따라 도식화 | 명령의 흐름을 쉽게 파악할 수 있지만 복잡한 알고리즘 표현에 한계 있음 |
| **프로그래밍 언어**를 이용한 구체화 | 프로그래밍 언어를 사용하여 표현 | 특정 언어를 모르면 이해하기 어렵고, 다른 언어로 변환 시 비효율적 |
| **가상코드**를 이용한 추상화 | 특정 프로그래밍 언어가 아닌 가상코드(Pseudo-code)로 표현 | 직접 실행할 수 없지만, 일반적인 프로그래밍 언어와 유사하여 특정 언어로 변환(구체화)이 쉬움 |

---

### **2. 순서도를 이용한 도식화**

순서도는 특정 기호를 사용하여 알고리즘의 실행 단계를 표현한다.

**순서도 기호**

| 기호 모양 | 의미 |
| --- | --- |
| 타원 | 시작 / 종료 |
| 직사각형 | 처리문, 치환문 |
| 평행사변형 | 입력 / 출력 |
| 마름모 | 조건문, 판단문 |
| 화살표 | 제어 흐름 |

---

### **3. 가상코드를 이용한 추상화**

가상코드는 의사코드 또는 알고리즘 기술 언어(ADL: Algorithm Description Language)로도 부른다.

**가상코드의 기본 요소**

- **기호** : 문자나 숫자를 조합한 것. 첫 글자는 반드시 영문자여야 함.
    - 변수, 자료형 이름, 프로그램 이름, 레코드의 필드 이름, 문장의 레이블 이름 등을 나타낼 때 사용.
    - 문장의 레이블 다음에는 콜론(`:`)을 입력해 수행할 문장과 구분.
- **자료형** : 정수형과 실수형의 수치 자료형, 문자형, 논리형, 포인터, 문자열 등 모든 자료형 사용 가능.
- **연산자** : 산술 연산자, 관계 연산자, 논리 연산자.

**지정문 (변수에 값 지정)**

- 지정 연산자(`←`)의 오른쪽 값을 왼쪽 영역(변수)에 저장.
- 명령문은 세미콜론(`;`)을 사용하여 끝을 표시하고, 한 행에 여러 개의 명령문을 쓸 수 있음.

```
변수 ← 값;

a ← 5;
a ← 3 + 2;
a ← b;
```

**조건문**

조건에 따라 실행할 명령문이 결정되는 선택적 제어 구조.

- **if 문**

```
if (조건식) then 명령문 1;
else 명령문 2;
```

- `if-then-else` 형 : 조건식이 참이면 명령문 1, 거짓이면 명령문 2 수행.
- `if-then` 형 : 조건식이 참이면 명령문 1 수행, 거짓이면 수행하지 않음.
- 중첩 if 문으로 다단계 조건식 표현 가능.

```
if (조건식 1) then 명령문 1;
else if (조건식 2) then 명령문 2;
else 명령문 3;
```

- **case 문** : 여러 조건식 중에서 해당하는 조건을 찾아 정해진 명령을 수행. `if` 문의 중첩과 의미가 같음.

```
case {
    조건식 1 : 명령문 1;
    조건식 2 : 명령문 2;
    ...
    조건식 n : 명령문 n;
    else     : 명령문 n+1;
}
```

**반복문**

일정한 명령을 반복하여 수행하는 루프(Loop) 형태.

- **for 문** : 초깃값을 시작으로 증감값에 따라 증가/감소하면서 조건식이 참인 동안 명령문을 반복 수행.

```
for (초깃값; 조건식; 증감값) do 명령문;
```

- **while-do 문** : 조건식을 검사하여 조건식이 참인 동안 명령문을 반복 수행.

```
while (조건식) do 명령문;
```

- **do-while 문** : while-do 문과 달리 조건식이 참이 아니더라도 명령문이 한 번은 수행됨.

```
do 명령문;
while (조건식);
```

**함수문**

- 전체 프로그램 중 같은 작업을 하나의 단위 프로그램으로 독립적으로 수행되게 한 것.
- 함수를 호출했을 때 독립적으로 실행되며 매개변수를 사용하여 다른 함수나 프로그램을 서로 연결.
- `return` 문 : 함수의 실행 결괏값을 호출한 위치로 반환.
- `end` 문 : 함수 실행을 마침.

```
함수 이름(매개변수)
    명령문;
    ...
    return 결괏값;
end
```

# Sec06 알고리즘의 성능 분석

---

### **1. 알고리즘 성능 분석 기준**

알고리즘 성능을 분석하는 판단 기준: **정확성, 명확성, 수행량, 메모리 사용량, 최적성**

- **정확성** : 올바른 자료가 입력되었을 때 올바른 결과를 출력할 수 있는지.
- **명확성** : 알고리즘이 얼마나 이해하기 쉽고 명확하게 작성되었는지.
- **수행량** : 기본적으로 포함되는 일반적인 연산을 제외하고, 알고리즘의 특성을 나타내는 주요 연산을 모두 분석.
- **메모리 사용량** : 알고리즘에 쓰이는 메모리 사용량도 판단 기준.
- **최적성** : 가장 중요한 기준. 알고리즘을 적용할 시스템의 사용 환경과 중요 요구사항이 다르기 때문에 조건에 맞는 ‘최적의’ 알고리즘이어야 함.

---

### **2. 알고리즘 성능 분석 방법**

알고리즘 성능 분석은 **공간 복잡도**와 **시간 복잡도**를 추정하여 수행한다.

**공간 복잡도 (Space Complexity)**

- 알고리즘을 프로그램으로 실행하여 완료하는 데까지 필요한 총 저장 공간.
- 구성 : **고정 공간** + **가변 공간**
    - 고정 공간 : 프로그램 크기나 입출력 횟수와 상관없이 고정적으로 필요한 저장 공간 (프로그램 저장 공간, 변수 및 상수 저장 공간).
    - 가변 공간 : 실행 과정에서 사용하는 자료와 변수를 저장하는 공간과 함수를 실행하는 데 관련 있는 정보를 저장하는 공간.

**시간 복잡도 (Time Complexity)**

- 알고리즘을 프로그램으로 실행하여 완료하는 데까지 소요되는 시간.
- 시간 복잡도 = **컴파일 시간** + **실행 시간**
- 컴파일 시간은 고정적이므로 의미 있는 시간으로 취급하지 않음.
- 실행 시간은 같은 프로그램이라도 실행되는 컴퓨터 성능에 따라 달라질 수 있기 때문에 **명령문의 실행 빈도수를 계산**하여 추정.
- 지정문, 조건문, 반복문에 있는 제어문과 `return` 문은 시간 차이가 거의 없으므로 하나의 단위 시간을 갖는 기본 명령문으로 취급하여 실행 빈도수를 계산.

---

### **3. 알고리즘 성능 분석 표기법**

알고리즘을 비교하기 위한 성능 분석 표기는 **시간 복잡도 표기**를 의미한다.

- 시간 복잡도는 실행 빈도 함수에서 입력 크기 n에 따라 증가율이 가장 큰 하나의 항에 대해 **차수 표기법(Order Notation)** 으로 표기.
- 함수의 상수항과 계수는 무시하고 n의 증가에 따라 증가율이 가장 큰 항 하나를 선택.
- 시간 복잡도 표기법 종류 : **빅–오 표기법, 빅–오메가 표기법, 빅–세타 표기법**

**빅–오 표기법 (Big-Oh Notation)**

- `O(f(n))`과 같이 표기하며, “Big Oh of f(n)”으로 읽는다.
- 실행 빈도 함수에서 구한 시간 복잡도가 n²이라면 `O(n²)`으로 표기.
- **함수의 상한**을 나타내기 위한 표기법 (최악의 경우).
- 수학적 정의 : 함수 f(n)과 g(n)이 주어졌을 때, 모든 n≥n₀에 대하여 |f(n)| ≤ c|g(n)|을 만족하는 상수 c와 n₀이 존재하면, f(n) = O(g(n))이다.

**빅–오메가 표기법 (Big-Omega Notation)**

- `Ω(f(n))`과 같이 표기하며, “Big Omega of f(n)”으로 읽는다.
- **함수의 하한**을 나타내기 위한 표기법 (최선의 경우).
- 어떤 알고리즘의 시간 복잡도가 Ω(g(n))으로 분석되었다면, 이 알고리즘 수행에는 적어도 g(n)의 수행 시간이 필요함을 의미.
- 수학적 정의 : 함수 f(n)과 g(n)이 주어졌을 때, 모든 n≥n₀에 대하여 |f(n)| ≥ c|g(n)|을 만족하는 상수 c와 n₀이 존재하면, f(n) = Ω(g(n))이다.

**빅–세타 표기법 (Big-Theta Notation)**

- `θ(f(n))`과 같이 표기하며, “Big Theta of f(n)”으로 읽는다.
- **상한과 하한이 같은 정확한 차수**를 표현하기 위한 표기법.
- f(n) = θ(g(n))이 되려면 f(n) = O(g(n))이면서 f(n) = Ω(g(n))이어야 한다.
- 수학적 정의 : 함수 f(n)과 g(n)이 주어졌을 때, 모든 n≥n₀에 대하여 c₁|g(n)| ≤ f(n) ≤ c₂|g(n)|을 만족하는 상수 c₁, c₂와 n₀이 존재하면, f(n) = θ(g(n))이다.

**세 가지 표기법 비교**

| 표기법 | 의미 | 표현 |
| --- | --- | --- |
| 빅–오 O(g(n)) | 상한 (최악의 경우) | f(n) ≤ c·g(n) |
| 빅–오메가 Ω(g(n)) | 하한 (최선의 경우) | f(n) ≥ c·g(n) |
| 빅–세타 θ(g(n)) | 상한 = 하한 (정확한 차수) | c₁·g(n) ≤ f(n) ≤ c₂·g(n) |

**주요 시간 복잡도 함수 크기 비교**

```
logn < n < nlogn < n² < n³ < 2ⁿ
```

| logn | n | nlogn | n² | n³ | 2ⁿ |
| --- | --- | --- | --- | --- | --- |
| 0 | 1 | 0 | 1 | 1 | 2 |
| 1 | 2 | 2 | 4 | 8 | 4 |
| 2 | 4 | 8 | 16 | 64 | 16 |
| 3 | 8 | 24 | 64 | 512 | 256 |
| 4 | 16 | 64 | 256 | 4096 | 65536 |
| 5 | 32 | 160 | 1024 | 32768 | 4294967296 |

> 실행 빈도수가 상수값이라면 O(1)로 표기하며 가장 빠른 실행 시간 함수가 된다.
>